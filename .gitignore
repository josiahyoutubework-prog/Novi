node_modules/
dist/
*.log
*.tsbuildinfo

# Secrets — never commit the API key
.env
server/.env

# SQLite database (regenerated + seeded on first boot)
server/data.db
server/data.db-shm
server/data.db-wal
